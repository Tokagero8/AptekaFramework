@model IEnumerable<AptekaFramework.Models.delivery>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.delivery_method)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.delivery_track_number)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.delivery_status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.delivery_method)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.delivery_track_number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.delivery_status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_delivery }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_delivery }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_delivery })
        </td>
    </tr>
}

</table>
